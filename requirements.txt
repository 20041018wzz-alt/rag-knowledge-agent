# 运行时依赖（基础安装即可跑通 Mock 模式全链路）
fastapi>=0.110
uvicorn>=0.29
httpx>=0.27
pydantic>=2.5
python-dotenv>=1.0

# ── 可选能力（按需安装，Docker 基础镜像默认不装）──
# pgvector 生产向量库驱动（不装 → 自动降级 Chroma → Memory）
# psycopg[binary]>=3.1
# psycopg_pool>=3.2
# 本地 BGE embedding（需能安装 torch）
# sentence-transformers>=3.0
# Chroma 向量库（本地开发 / 单机 Demo 用）
# chromadb>=0.5
# PDF 文档解析
# pdfplumber>=0.11
